// * Geral
// 1. Defina o que é uma variável na programação.
// Uma variável é um espaço de armazenamento nomeado que pode conter um valor. Na programação, é usada para representar informações que podem ser modificadas durante a execução do programa

// 2. Escreva com as suas palavras a diferença entre as declarações const, let e var e como elas podem
// ser aplicadas na prática.
// const é preferível para valores que não mudam, let para variáveis que precisam ser atualizadas e var é menos utilizado hoje em dia devido aos escopos menos controlados.

// 3. Explique a diferença entre as variáveis do tipo objeto e lista.
// Variáveis do tipo objeto: Em muitas linguagens de programação, um objeto é uma estrutura de dados que contém dados e métodos. Ele é usado para modelar coisas que têm múltiplas propriedades ou métodos associados a elas. Em JavaScript, objetos são coleções de pares chave-valor e são usados para representar estruturas mais complexas.
// Variáveis do tipo lista (ou array): Uma lista (ou array) é uma estrutura de dados que contém uma coleção ordenada de elementos, cada um identificado por um índice. Em JavaScript, arrays são usados para armazenar múltiplos valores em uma única variável, permitindo acessar esses valores de maneira eficiente por meio de índices numéricos.






//Pesquisar o que é 
// Lista
// Array
// Vetor